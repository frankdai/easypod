{
  "rules": {
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    },
    "channels": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
